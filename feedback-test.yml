config:
  target: "http://localhost:3000"
  phases:
    - duration: 60
      arrivalRate: 30
scenarios:
  - flow:
    - post:
        url: "/api/Feedbacks"
        json:
          comment: "This site is slow!"
          rating: 1
